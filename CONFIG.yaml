# Update the path to this CONFIG.yaml file in function load_config of module train.py

PATH :
  DATA_DIR : "/home/colind/work/Mines/TR_DIMA/DIMA_code/data"

DATA :
  TOTAL_N_CHANNELS : 111
  NUMBER_OF_CHANNELS : 10

  TEST_SIZE : 0.15 # for random train/test split
  TEST_LEAVES : [1, 3, 7, 12]
  VALIDATION_LEAVES : [2, 4, 8, 11]
  NUMBER_OF_LEAVES : 17

TRAINING_CHOICE :
  DATA_TYPE : "lab_mask"  #"lab_mask"  # or "dist_mask" ring_mask
  MODEL_TYPE : "MLP"  # "MLP"  # or "TREE"
  BALANCE : False # proportion of sick pixels after transformation. no balance : False
  NORMALISE : False

TRAINING_INFO :
  DEVICE : "cuda"  # or "cpu"
  # for Neural Networks
  LAB_MASK :
    MLP :
      NUM_EPOCHS : 300
      HIDDEN_SIZE : 50 
      LEARNING_RATE : 0.3
      FACTOR : 0.5
      PATIENCE : 20
      THRESHOLD : 0.001
      LABEL_THRESHOLD : 0.75
      DELTA : 0.0001
    
    TREE :
      # for decision tree
      MAX_DEPTH : 4  # For no limit : null
      CHANNELS : [64, 68, 65]
  
  DIST_MASK :
    MLP :
      NUM_EPOCHS : 2000
      HIDDEN_SIZE : 50
      LEARNING_RATE : 0.001
      FACTOR : 0.5
      PATIENCE : 20
      THRESHOLD : 0.1
      DELTA : 0.0001
  
  RING_MASK :
    RANDOM_FOREST :
      CHANNELS : [0,1,2,3,4,65,64,5,66,63,67,62,68,6,69,61,70,7,71,27,28,60,72,26,29]
      N_ESTIMATORS : 100

    MLP :
      NUM_EPOCHS : 1000
      HIDDEN_SIZE : 50 
      LEARNING_RATE : 0.3
      FACTOR : 0.5
      PATIENCE : 20
      THRESHOLD : 0.001
      LABEL_THRESHOLD : 0.75
      DELTA : 0.0001      

  
