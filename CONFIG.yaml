# Update the path to this CONFIG.yaml file in function load_config of module train.py

PATH :
  DATA_DIR : "/home/colind/work/Mines/TR_DIMA/DIMA_code/data"

DATA :
  TOTAL_N_CHANNELS : 111
  NUMBER_OF_CHANNELS : 111
  BALANCE : False # proportion of sick pixels after transformation. no balance : False
  TEST_SIZE : 0.15 # for random train/test split
  TEST_LEAVES : [7, 1, 3, 12]
  VALIDATION_LEAVES : [8, 11, 4, 2]
  NUMBER_OF_LEAVES : 17

TRAINING_CHOICE :
  DATA_TYPE : "lab_mask"  # or "dist_mask"
  MODEL_TYPE : "MLP"  # or "tree"

TRAINING_INFO :
  DEVICE : "cuda"  # or "cpu"
  # for Neural Networks
  LAB_MASK :
    MLP :
      NUM_EPOCHS : 1000
      HIDDEN_SIZE : 50 
      LEARNING_RATE : 0.3
      FACTOR : 0.5
      PATIENCE : 20
      THRESHOLD : 0.001
      LABEL_THRESHOLD : 0.75
    
    TREE :
      # for decision tree
      MAX_DEPTH : 4  # For no limit : null
      CHANNELS : [64, 68, 65]
  
  DIST_MASK :
    MLP :
      NUM_EPOCHS : 2000
      HIDDEN_SIZE : "HALF"
      LEARNING_RATE : 0.001
      FACTOR : 0.5
      PATIENCE : 20
      THRESHOLD : 0.1

  
